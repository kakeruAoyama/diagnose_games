<div id="<%= dom_id game %>">
  <p>
    <strong>Name:</strong>
    <%= game.name %>
  </p>

  <p>
    <strong>Discription:</strong>
    <%= game.discription %>
  </p>

  <% game.tags.each do |tag| %>
    <span><%= tag.name %></span>
  <% end %>

</div>
